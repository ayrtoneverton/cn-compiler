#include <stdio.h>

int main(int argc, const char *argv[]){
	int x, y, c = 0;
	printf("Enter value X: ");
	scanf("%d", &x);
	printf("Enter value Y: ");
	scanf("%d", &y);

	printf("x * x - y + c = %d", x * x - y + c);
	return 0;
}
